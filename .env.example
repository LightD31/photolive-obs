# Configuration PhotoLive OBS
# Copiez ce fichier en .env et ajustez les valeurs selon vos besoins

# Port du serveur (par défaut: 3001)
PORT=3001

# Origines autorisées pour CORS (séparées par des virgules)
# Laissez vide pour utiliser les valeurs par défaut (localhost)
ALLOWED_ORIGINS=http://localhost:3001,http://127.0.0.1:3001

# Niveau de log (error, warn, info, debug)
LOG_LEVEL=info

# Taille maximale des requêtes JSON (par défaut: 1mb)
MAX_REQUEST_SIZE=1mb

# Chemin par défaut des photos (optionnel)
# DEFAULT_PHOTOS_PATH=./photos
